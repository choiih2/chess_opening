# 체스 오프닝 트레이너 개발 과정 — 생성형 AI 협업 기록

> 자기소개서 "3. 프로젝트, 학업, 활동 등 생성형 AI 툴을 활용하여 학습이나 업무 효율성을
> 극대화했던 경험을 구체적으로 서술해주세요." 항목 작성을 위해, Claude Code(Anthropic의
> 에이전틱 코딩 CLI)와 함께 체스 오프닝 학습 웹앱을 처음부터 끝까지 만든 전 과정을 정리한
> 문서입니다. 실제 대화 흐름을 그대로 복원했으며, 프롬프트 하나하나가 아니라 "무엇을
> 판단하고, 무엇을 검증했는지"를 중심으로 서술했습니다.

---

## 0. 프로젝트를 시작한 이유

체스 오프닝(초반 정석)을 체계적으로 공부하고 싶었지만, 시중의 오프닝 트레이너 앱(로투스
체스 등)은 대부분 유료 구독형이었습니다. 그런데 이런 앱들이 파는 핵심 데이터 — 실전
승률 통계, 대국 기록, 엔진 분석 — 은 사실 Lichess와 Chess.com이 **무료 공개 API**로,
Stockfish는 **오픈소스 엔진**으로 이미 공개하고 있었습니다. "유료 앱이 파는 건 데이터
자체가 아니라 그걸 조합해 보여주는 UI"라는 판단이 서서, 직접 만들기로 했습니다.

문제는 저 혼자서는 프론트엔드 아키텍처 설계, 3개 외부 API 연동, 브라우저에서 체스 엔진을
직접 돌리는 WASM/Web Worker 통합까지 전부 감당하기엔 시간이 부족했다는 점입니다. 이
과정 전체를 Claude Code와의 협업으로 진행했고, 아래는 그 기록입니다.

---

## 1. 개발 타임라인

### 1단계 — 기획: "AI에게 무엇을 시켜야 하는가"부터 시작

가장 처음 한 일은 코드를 짜 달라고 하는 게 아니라, **"이 프로젝트를 준비하려면 어떤
프롬프트를 줘야 하는지"를 AI에게 되물은 것**이었습니다. 그 결과 얻은 핵심 조언은:

- 한 번의 거대한 프롬프트로 완성품을 요구하면 대부분 실패한다. **단계별로 쪼개서, 각
  단계마다 실제로 동작하는 상태를 만드는 것**이 훨씬 안정적이다.
- 무료로 재현 가능한 재료를 먼저 목록화한다: 수 검증엔 chess.js, 보드 UI엔
  react-chessboard, 오프닝 통계엔 Lichess Opening Explorer API(키 불필요), 엔진
  분석엔 Stockfish WASM, 저장엔 IndexedDB.

경쟁 앱인 "로투스 체스"를 예로 들어 실제 기능을 분석하게 시켰더니, AI가 웹 검색으로 그
앱의 실제 셀링포인트(예: "실전에서 이기는 라인 우선 추천")를 조사한 뒤, **그 기능이
사실 Lichess Explorer API의 통계를 그대로 활용하면 재현 가능하다는 것**을 표로
정리해줬습니다. 이 단계에서 "내가 만들려는 게 기술적으로 타당한지"를 코드 한 줄 없이
먼저 검증할 수 있었습니다.

### 2단계 — 초기 스캐폴딩

React + Vite + TypeScript 구조로 시작해 다음을 구현했습니다.

- 체스판 렌더링, 합법수만 드래그 가능한 인터랙션
- 변화수(branch)를 지원하는 기보 트리 구조
- PGN 붙여넣기로 라인 불러오기 / 내보내기
- 백/흑으로 나눈 레퍼토리 저장·불러오기 (IndexedDB)

### 3단계 — Lichess API 연동 중 실전 트러블슈팅

오프닝 통계와 인기순 정렬을 붙이는 과정에서 **401 인증 오류**가 발생했습니다. 원인을
함께 진단한 결과, Lichess가 DDoS 공격을 받은 뒤 정책을 바꿔 Opening Explorer API에
인증을 요구하게 된 것으로 확인됐습니다 (AI의 학습 시점 이후에 일어난 변경이라 사전
지식으로는 알 수 없었던 부분 — 실시간으로 원인을 함께 추적해 해결).

- OAuth 토큰 발급 절차 안내
- **토큰이 클라이언트 코드에 노출되지 않도록**, Vite 개발 서버 프록시가 대신 헤더에
  토큰을 주입하는 구조로 설계 (`vite.config.ts`)
- 오프닝 이름 한글화: 구절 단위 사전(약 90개) + 낱말 단위 사전(약 200개)을 조합해
  영문 오프닝 이름 3,800여 개를 최대한 한글로 옮기는 번역 로직 구현

### 4단계 — UX 구조 개편: 학습 모드 / 연습 모드 분리

처음엔 오프닝 트리 탐색과 실전 연습이 한 화면에 뒤섞여 있었습니다. "메인 화면에서
학습과 연습을 먼저 선택하고, 그다음 오프닝을 고르게 해 달라"고 요청해 다음을 구현:

- 모드 선택 화면(`ModeSelect`) 신설
- 학습 모드에서는 실전 통계상 "메인라인"에 금색 배지를 붙이고, 하위 변화를 실전
  빈도순으로 정렬해 "어디로 갈라지는지"가 한눈에 보이게 개편

### 5단계 — 체스 엔진(Stockfish) 연동: 실시간 힌트 기능

연습 모드에서 "메인라인"과 "엔진이 보기에 확실히 더 좋은 수(탁월한 수)"를 화살표로
보여주는 기능을 요청했습니다. 이 단계에서 AI가 한 일:

- npm 패키지를 실시간 웹 검색으로 조사해, **Stockfish 18의 "lite/싱글스레드" 빌드가
  브라우저의 특수 보안 헤더(COOP/COEP) 없이도 동작하며 크기가 약 7MB로 실용적**이라는
  사실을 확인 후 정확한 패키지·버전(`stockfish@18.0.8`)을 선정
- Web Worker + UCI(엔진 통신 프로토콜) 레이어를 직접 설계·구현 (`src/lib/engine.ts`):
  MultiPV(후보 수 여러 개 동시 분석), 후보 수 간 평가 격차가 일정 기준(1.5폰) 이상이면
  "탁월한 수"로 판정하는 휴리스틱
- 분석 결과를 FEN 단위로 IndexedDB에 캐싱해 같은 국면을 재분석하지 않도록 최적화
- 빌드 스크립트(`scripts/copy-engine.mjs`)로 엔진 파일을 자동 배치하도록 구성

### 6단계 — Chess.com 연동: 실전 기보 복기 기능 (가장 큰 작업)

"내가 실제로 뒀던 경기를 다시 보고, 상대가 뒀던 수를 재현하면서 더 좋은 수와 함정
가능 지점을 알려주는 기능"을 요청했습니다. 규모가 크다고 판단한 AI가 **먼저 설계
계획(Plan Mode)을 세워 검토받은 뒤 구현에 들어가는 절차**를 제안했고, 이를 승인한 뒤
다음을 구현했습니다.

- Chess.com 공개 API 리서치 및 연동 (로그인 불필요, 아이디만으로 공개 대국 조회)
- 월별 대국 아카이브 수집·캐싱, 최신 달 우선 로드, "더 보기"로 과거 달 확장
- PGN 파싱 후 보유 중인 오프닝 데이터베이스와 자동 매칭해 오프닝명 표시
- **한 판 전체를 첫 수부터 순서대로 엔진에 태워, 수마다 블런더/실수/부정확/정확
  4단계로 자동 채점**하는 분석 파이프라인 (`src/lib/gameAnalysis.ts`)
- "함정 지점" 탐지: 실제로 둔 수와 엔진이 보기에 확실히 더 좋았던 수가 다르면 표시하고,
  그게 내 실수였는지 상대 실수였는지까지 구분
- 이 작업으로 새 파일 10개 이상(`chesscom.ts`, `gameAnalysis.ts`, `GameList.tsx`,
  `GameBoard.tsx`, `GameReview.tsx` 등) 추가

### 7단계 — 실사용 기반 개선 (5개 기능 동시 요청)

앱을 직접 써보면서 나온 요구사항을 한 번에 정리해 요청했고, 이를 모두 구현했습니다.

1. **모든 모드에 되돌리기 기능** — 학습/연습 공용 보드에 처음/이전/다음/마지막 이동,
   연습 모드엔 "이전 수" (내 수 + 상대 응수 취소 후 재시도)
2. **복기 모드 함정 지점 요약** — 하나씩 클릭해야 보이던 걸, 분석 완료 시 "찾은 함정
   지점 N곳" 목록으로 한 번에 노출
3. **오프닝 즐겨찾기** — 트리·패널에 별 토글, 즐겨찾기 고정 섹션
4. **"정석 범위 이탈 시 세션이 멈추는" 문제 해결** — 아래 3장에서 상세 서술
5. **"실수 유도" 난이도 신설** — 스카치 갬빗·블루멘펠트처럼 상대의 흔한 실수를
   응징하는 공격을 연습할 수 있도록, 상대 입장 승률이 낮은 수를 오히려 더 자주 두게
   하는 가중치 로직 추가 (기존 "함정 대비" 난이도의 정반대 가중치를 재사용해 최소
   코드로 구현)

---

## 2. 생성형 AI를 "제대로" 활용했다고 말할 수 있는 지점들

자기소개서에 쓸 핵심은 여기입니다. **AI가 코드를 생성했다는 사실 자체가 아니라, 그
결과물을 어떻게 검증하고 판단에 개입했는지**가 이 프로젝트에서 반복적으로 드러났습니다.

### 2.1 "결과물을 무조건 신뢰하지 않는" 검증 루프

새 기능을 넣을 때마다 타입 체크(`tsc`)만으로 끝내지 않고, **헤드리스 브라우저
(Playwright)를 직접 띄워 버튼을 클릭하고, 콘솔 에러까지 확인하는 절차**를 거쳤습니다.
이 과정에서 실제로 잡아낸 버그들:

- **표기 오류**: 체스 기보(SAN)가 화면에 대문자로 깨져 보이는 버그를 발견 — 원인은
  "라벨용 CSS 클래스(대문자 변환 스타일)를 실제 데이터 표시에 재사용"한 것이었고,
  전용 스타일을 분리해 해결.
- **자기 자신의 판단 오류를 재검증으로 뒤집은 사례**: 엔진 분석이 "90초 넘게 걸린다"고
  1차 결론 내렸는데, 재검증 과정에서 원인이 실제 엔진 성능이 아니라 **테스트
  스크립트 자체의 API 사용 실수(타임아웃 인자 순서 오류)**였음을 밝혀내고, 정확한
  실측치(약 30초, 애초 안내한 수치와 일치)로 정정. 잘못된 결론을 그대로 사용자에게
  전달하지 않고 재검증해서 바로잡은 케이스.
- **엣지 케이스 버그**: "이전 수" 되돌리기 버튼이 "상대가 먼저 두는 국면"에서는 내
  수가 없는데도 활성화되어, 누르면 상대 수만 취소되고 조용히 다른 수로 재시도되는
  비직관적 동작을 실제 클릭 테스트로 발견 → 되돌릴 내 수가 실제로 있을 때만 버튼이
  활성화되도록 조건을 수정.

이 세 사례 모두 "타입 에러 없음 = 정상 동작"이 아니라는 걸 보여주는 근거로, **AI
산출물을 실제 환경에서 재현해 검증하는 습관**을 자기소개서에서 구체적 일화로 쓸 수
있습니다.

### 2.2 설계를 통째로 위임하지 않고 "선택지 비교 후 결정"

"정석 범위를 벗어나면 연습 세션이 멈추는" 문제를 해결할 때, AI에게 곧바로 구현을
시키지 않고 **가능한 해결책 4가지를 비교표로 요청**했습니다.

| 방법 | 설명 | 채택 여부 |
|---|---|---|
| 레이팅대 확장 후 재시도 | 통계가 없으면 더 넓은 레이팅대로 재조회 | 채택 (1차) |
| 엔진이 상대 역할 인수 | 그래도 없으면 Stockfish가 대신 둠 | 채택 (2차) |
| 완전 무작위 수 | 구현은 쉽지만 연습 가치가 낮음 | 기각 |
| 양쪽 다 직접 두게 함 | 자동 진행을 포기 | 기각 |

두 방법을 조합해 채택했고, 이렇게 **여러 대안의 트레이드오프를 표로 정리시킨 뒤
직접 판단해 조합하는 방식**은 AI를 "생각 없이 실행하는 도구"가 아니라 "옵션을
빠르게 펼쳐 보여주는 리서치 파트너"로 쓰는 방법을 보여줍니다.

### 2.3 규모가 큰 기능은 계획 먼저, 구현은 나중에

Chess.com 연동처럼 여러 파일에 걸친 큰 기능을 요청했을 때, AI가 스스로 "먼저 설계
계획을 세워 검토받겠다"고 제안하고 구현 전 계획 문서를 작성해 승인을 받은 뒤 착수했습니다.
이 절차 덕분에 "만들고 나서 방향이 틀렸다"는 재작업 리스크를 사전에 줄일 수 있었습니다.

### 2.4 최신 정보가 필요한 순간엔 AI의 지식보다 실시간 검색을 우선시켰다

Stockfish 패키지 버전 선정, Chess.com API의 정확한 응답 필드명, Lichess 정책 변경
사실 확인 등 **AI 자체 지식이 최신이 아닐 수 있는 영역은 실시간 웹 검색으로 재확인한
뒤 결정하도록 요구**했습니다. 이는 "AI가 아는 대로 믿는다"가 아니라 "AI의 리서치
능력을 활용해 사실을 확인시킨다"는 활용 방식입니다.

---

## 3. 결과 요약 (정량)

- **모드 3개**: 학습 모드 / 연습 모드 / 복기 모드
- **외부 API 3개 연동**: Lichess Opening Explorer, Chess.com 공개 API, Stockfish
  엔진(WASM, 로컬 실행이라 API는 아니지만 별도 통합 난이도가 있는 외부 엔진)
- **연습 난이도 5종**: 실전 빈도 / 메인라인 / 함정 대비 / 완전 랜덤 / 실수 유도
- **전 구간 무료 스택**: 유료 API·구독 없이(선택적 AI 요약 기능 제외) 상용 앱 수준
  기능을 무료 오픈소스 조합만으로 구현
- **라이브러리 코드 20여 개 모듈, 컴포넌트 10여 개**로 구성된 프론트엔드 단일
  페이지 앱
- 매 기능 추가마다 **헤드리스 브라우저 실제 구동 테스트**를 거쳐 최소 3건의 실제
  버그를 배포 전에 발견·수정

---

## 4. 자기소개서 초안 (참고용, 분량별)

### 짧은 버전 (약 500자)

기존 체스 오프닝 학습 앱이 대부분 유료 구독형이라는 점에 착안해, Lichess·Chess.com의
무료 공개 API와 오픈소스 체스 엔진 Stockfish만으로 동일한 기능을 하는 웹앱을 생성형
AI 코딩 도구(Claude Code)와 협업해 직접 개발했습니다. 단순히 코드를 생성시키는 데
그치지 않고, 기능을 추가할 때마다 헤드리스 브라우저로 실제 클릭 동작과 콘솔 에러까지
검증하는 루프를 요구해, 표기 오류·엣지 케이스 버그·잘못된 성능 측정 등 실제 결함
여러 건을 배포 전에 찾아 고쳤습니다. 규모가 큰 기능(실전 기보 자동 분석)은 구현 전에
설계안을 먼저 정리해 검토한 뒤 착수했고, 설계 판단이 필요한 지점에서는 AI에게 여러
대안을 비교표로 제시하게 한 뒤 직접 조합해 결정했습니다. 이 경험을 통해 생성형 AI를
"코드를 대신 써주는 도구"가 아니라 "검증 가능한 결과를 빠르게 만들어내는 협업
파트너"로 다루는 법을 익혔습니다.

### 중간 버전 (약 1,000자)

체스 오프닝을 공부하려 했지만 기존 학습 앱이 대부분 유료였습니다. 앱이 파는 것이
데이터 자체가 아니라 무료 공개 데이터(Lichess 오프닝 통계, Chess.com 대국 기록)를
조합해 보여주는 UI라는 판단이 서서, 생성형 AI 코딩 도구인 Claude Code와 협업해
동일한 기능의 웹앱을 처음부터 직접 만들었습니다.

개발은 단계별로 진행했습니다. 오프닝 트리·레퍼토리 저장 같은 기본기부터 시작해,
Lichess API 인증 정책 변경으로 발생한 401 오류를 함께 진단해 프록시 기반 보안 설계로
해결했고, 오프닝 이름 한글화 사전을 직접 설계했습니다. 이후 브라우저에서 Stockfish
엔진을 Web Worker로 직접 구동하는 힌트 기능, Chess.com 공개 API로 실전 기보를
불러와 한 수씩 자동 채점하고 "놓친 함정 지점"을 짚어주는 복기 기능까지 확장했습니다.

이 과정에서 가장 신경 쓴 것은 AI 산출물을 그대로 믿지 않는 태도였습니다. 기능을 추가할
때마다 헤드리스 브라우저를 실제로 띄워 클릭해보고 콘솔 에러까지 확인하는 절차를
거쳤고, 이 과정에서 CSS 재사용으로 인한 표기 오류, "상대가 먼저 두는 국면"에서만
드러나는 되돌리기 버튼의 엣지 케이스 버그를 실제로 잡아 고쳤습니다. 특히 한 번은 AI
스스로 "분석이 90초 넘게 걸린다"고 판단했다가, 재검증 끝에 원인이 엔진 성능이 아니라
테스트 스크립트 자체의 실수였음을 밝혀내고 정정하는 과정도 있었습니다. 설계 판단이
필요한 지점(예: 정석 범위를 벗어난 연습 세션이 멈추는 문제)에서는 가능한 해결책을
비교표로 먼저 정리시킨 뒤 직접 조합해 채택했습니다.

이 프로젝트로 생성형 AI를 코드 자동완성 도구가 아니라, 리서치·구현·검증을 아우르는
협업 파트너로 다루는 법을 배웠습니다. 결과물을 무조건 신뢰하지 않고 실제 환경에서
재현·검증하는 습관, 설계 결정을 통째로 위임하지 않고 대안을 비교해 직접 판단하는
태도가 이 경험에서 얻은 가장 큰 자산입니다.

---

## 5. 부록 — 최종 기술 스택

- **프론트엔드**: React 18, TypeScript, Vite
- **체스 로직**: chess.js, react-chessboard
- **엔진**: Stockfish 18 (lite/싱글스레드 WASM 빌드), Web Worker + UCI 프로토콜
- **데이터 소스**: Lichess Opening Explorer API, Lichess chess-openings 데이터셋,
  Chess.com 공개 API
- **저장소**: IndexedDB (자체 경량 래퍼)
- **테스트/검증**: Playwright(헤드리스 Chromium) 기반 실제 구동 테스트
- **선택적 기능**: Anthropic API(있으면 오프닝 핵심 아이디어를 AI가 한글로 요약,
  없어도 나머지 기능은 전부 정상 동작)
